import React, { useState } from "react";
import DataPlans from "./DataPlansCards";
import DataPlanCard from "./DataPlansModal";

const EsimSale = () => {

  return (
    <div>
      {/* <DataPlans />
      <DataPlanCard setShowModal={setShowModal} showModal={showModal} /> */}
    </div>
  );
};

export default EsimSale;
